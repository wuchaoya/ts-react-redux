import * as React from 'react';

export default class CallMe extends React.PureComponent {
  
  constructor (props: object) {
    super(props);
  }
  
  public render () {
    return (
      <div>callMe</div>
    )
  }
  
}